<%# 王冠。Phosphor に同ウェイトの既製品が無いので、Light と同じ線幅(12)で自作した線画。
    1em で描いても山と谷が潰れないよう、谷は深く・台座は低くしている。
    .icon の fill: currentColor は継承なので、内側の属性で打ち消して stroke で描く。 %>
<svg class="icon <%= local_assigns[:class] %>" viewBox="0 0 256 256" aria-hidden="true" focusable="false"><g fill="none" stroke="currentColor" stroke-width="12" stroke-linecap="round" stroke-linejoin="round"><path d="M44,84 84,154 128,50 172,154 212,84 198,200 58,200 Z"/></g></svg>
